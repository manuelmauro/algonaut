/*
 * Algod REST API.
 *
 * API endpoint for algod operations.
 *
 * The version of the OpenAPI document: 0.0.1
 * Contact: contact@algorand.com
 * Generated by: https://openapi-generator.tech
 *
 * Renamed from the generated `TransactionParams200Response` and given
 * domain-typed fields via `openapi/type-overrides.json` (D5 of ADR
 * relocate-generated-models). Replaces the hand-written
 * `client_types::SuggestedParams` wrapper, which this fully reproduces.
 */

use serde::{Deserialize, Serialize};

use crate::client_types::TransactionParams;
use algonaut_core::{MicroAlgos, Round};
use algonaut_crypto::{HashDigest, deserialize_hash};

/// SuggestedParams : TransactionParams contains the parameters that help a client construct a new transaction.

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct SuggestedParams {
    /// ConsensusVersion indicates the consensus protocol version as of LastRound.
    #[serde(rename = "consensus-version")]
    pub consensus_version: String,
    /// Fee is the suggested transaction fee Fee is in units of micro-Algos per byte. Fee may fall to zero but transactions must still have a fee of at least MinTxnFee for the current network protocol.
    #[serde(rename = "fee")]
    pub fee: MicroAlgos,
    /// GenesisHash is the hash of the genesis block.
    #[serde(rename = "genesis-hash", deserialize_with = "deserialize_hash")]
    pub genesis_hash: HashDigest,
    /// GenesisID is an ID listed in the genesis block.
    #[serde(rename = "genesis-id")]
    pub genesis_id: String,
    /// LastRound indicates the last round seen
    #[serde(rename = "last-round")]
    pub last_round: Round,
    /// The minimum transaction fee (not per byte) required for the txn to validate for the current network protocol.
    #[serde(rename = "min-fee")]
    pub min_fee: MicroAlgos,
}

impl SuggestedParams {
    /// TransactionParams contains the parameters that help a client construct a new transaction.
    pub fn new(
        consensus_version: String,
        fee: MicroAlgos,
        genesis_hash: HashDigest,
        genesis_id: String,
        last_round: Round,
        min_fee: MicroAlgos,
    ) -> SuggestedParams {
        SuggestedParams {
            consensus_version,
            fee,
            genesis_hash,
            genesis_id,
            last_round,
            min_fee,
        }
    }
}

impl TransactionParams for SuggestedParams {
    fn last_round(&self) -> u64 {
        self.last_round.0
    }

    fn min_fee(&self) -> u64 {
        self.min_fee.0
    }

    fn genesis_hash(&self) -> HashDigest {
        self.genesis_hash
    }

    fn genesis_id(&self) -> &String {
        &self.genesis_id
    }
}
